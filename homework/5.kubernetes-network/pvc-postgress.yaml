apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-redmine-pvc
  namespace: l5
  labels:
    app: postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi